import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-demo-cities',
  imports: [RouterLink],
  templateUrl: './demo-cities.html',
  styleUrl: './demo-cities.scss',
})
export class DemoCities {}
